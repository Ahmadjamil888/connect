# project_operator
Build-and-ship orchestration for IMOS. Auto-selects an installed coding agent or IDE, generates a precise implementation brief, preserves session context across follow-ups and target switches, verifies local artifacts, and when possible pushes to GitHub and deploys with configured providers.
